# RGZ: Идентификация диктора (one‑shot learning, triplet loss)\n\nЭтот модуль реализует вариант **Блок 4, вариант 50 — Идентификация диктора**:\n\n> Система верификации голоса (\"one‑shot learning\") — определение, принадлежит ли речь одному и тому же человеку. Используется подход с **triplet loss** и эмбеддингами диктора.\n\n## Структура\n\n- `config.py` — основные пути и гиперпараметры.\n- `audio_dataset.py` — PyTorch‑датасет, который ожидает структуру данных:\n  - `rgznemoe/speaker_verification/data/test1/<speaker_id>/*.wav`\n- `model.py` — CNN‑модель, которая строит L2‑нормированные эмбеддинги по mel‑спектрограммам.\n- `train.py` — обучение модели с triplet loss.\n- `verify.py` — скрипт верификации двух аудиофайлов (вычисление косинусного сходства эмбеддингов).\n- `app.py` — простое веб‑приложение на Gradio для демонстрации работы (загрузка двух аудио и ответ: один диктор или разные).\n- `requirements.txt` — основные зависимости.\n\n## Подготовка данных\n\nОжидаемая структура датасета `test1`:\n\n```text\nrgznemoe/speaker_verification/data/test1/\n    speaker_01/\n        0001.wav\n        0002.wav\n        ...\n    speaker_02/\n        0001.wav\n        ...\n    ...\n```\n\nКаждая папка соответствует отдельному диктору. Можно использовать подмножество VoxCeleb (предварительно сконвертировав в WAV).\n\n## Установка зависимостей\n\nВ (виртуальном) окружении выполните:\n\n```bash\npip install -r rgznemoe/speaker_verification/requirements.txt\n```\n\nУбедитесь, что установлены совместимые версии `torch` и `torchaudio` для вашей системы.\n\n## Обучение модели\n\n```bash\npython -m rgznemoe.speaker_verification.train \\\n  --data_root rgznemoe/speaker_verification/data/test1 \\\n  --model_path rgznemoe/speaker_verification/models/speaker_embedding_cnn.pt \\\n  --batch_size 32 --epochs 20 --lr 1e-3 --margin 0.5\n```\n\n## Верификация диктора из CLI\n\n```bash\npython -m rgznemoe.speaker_verification.verify \\\n  rgznemoe/speaker_verification/examples/ref.wav \\\n  rgznemoe/speaker_verification/examples/test.wav \\\n  --model_path rgznemoe/speaker_verification/models/speaker_embedding_cnn.pt\n```\n\nСкрипт выведет косинусное сходство эмбеддингов и текстовый вердикт.\n\n## Демонстрационное приложение (GUI)\n\n```bash\npython -m rgznemoe.speaker_verification.app\n```\n\nОткроется интерфейс Gradio в браузере, где можно загрузить две записи и получить ответ о совпадении диктора.\n\nВ пояснительной записке (`rgz_*.ipynb`) следует:\n\n- описать датасет `test1` и схему предобработки аудио (mel‑спектрограммы, MFCC и т.п.);\n- детально разобрать архитектуру модели и выбор triplet loss;\n- привести графики обучения и метрики на тестовой выборке;\n- показать примеры успешной и неуспешной верификации;\n- приложить ключевые фрагменты кода (остальной код уже содержится в данном модуле).\n+\n*** End Patch  `}  `}] */
